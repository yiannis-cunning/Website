" use :set list to view hidden characters
" use : ctrl + v in insert mode to insert literal characters (ie tab)

" Set line numbers on 
:set number

" Set syntax highlighting 
:syntax on


" Set tab to be 4x spaces
:set tabstop=4
:set shiftwidth=4
:set expandtab

